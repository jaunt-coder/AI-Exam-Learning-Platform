# WO-20260722-012 — 2018 answer.hwp VERIFY_EXPORT + ACC Q41–Q80 Answer Join

| Field | Value |
|-------|-------|
| handoff_id | `WO-20260722-012` |
| from | 00_Project_Navigator |
| to | Human (VERIFY_EXPORT) → 02_Engineer (join) → 03_Verification → ★ Gate B |
| sprint | `KS-ACC-LOSSLESS-GOLDEN` |
| stage | Pilot unblock — **VERIFY_EXPORT** + **answer join** |
| parent | `WO-20260722-011` (Scope Lock 유지) |
| depends_on | `WO-009` inventory · `WO-010` DESIGN ACCEPT · 011 pilot candidates |
| created_at | 2026-07-23 |
| current_mode | EXAM |
| exam_impact_score | **5** |
| study_roi | HIGH |
| recommended_action | DO_NOW |
| status | **CLOSED — JOIN 40/40 · VERIFY_EXPORT attested · Q69=5** |
| plane_touch | none |
| authority_risk | low |

---

## Goal

2018 `answer.hwp`에 대해 WO-010 **D-primary VERIFY_EXPORT**를 완료하고,  
ACC **Q41–Q80** candidate에 정답을 **questionNo join**으로 연결·검증한다.

---

## Scope Lock (WO-011 유지 · 바인딩)

### IN
- 2018 `answer.hwp` Human Export + attestation
- 2018 ACC candidate Q41–Q80 answer join
- staging only (`raw/hwp-export` · `raw/attested` · pilot candidate patch)
- join 품질 검증 (Validation 기준 4 해소)

### OUT (변경 없음)
- 2019–2026 연도 확장
- Pattern DB
- Question DB Product Persist
- D3/D4
- Parser 수정
- OCR
- source 수정 / convert / rename
- exam_2 재추출(본 WO 비범위; 011 산출 재사용)
- HWP→PDF / LibreOffice Golden
- Probe(A) 결과를 attested/Golden으로 승격

---

## Current Blocker (사실)

| Item | State |
|------|-------|
| `data/knowledge/raw/hwp-export/**` | **없음** |
| A-probe (`answer.hwp.probe-meta.json`) | `ok=false` · join **0/40** |
| Candidates `ACC_2018_Q041`–`Q080` | `answer: null` · `verified: false` |
| Validation #4 (정답 연결) | **HOLD** |

Probe는 Golden 경로 **아님**. 본 WO는 **D Human Export**만 공식으로 인정한다.

---

## Immutable Source Ref (WO-009)

| role | path | sha256 |
|------|------|--------|
| answer | `source/past-exams/2018/answer.hwp` | `1a67082b336e263b4b22873696be2d7e949027bbba4e6a5bcb9fc4d826ab2595` |

---

## Work Order

```text
## Work Order
- goal: VERIFY_EXPORT 2018 answer.hwp + join ACC Q41–Q80 answers
- why_now: 011 pilot candidates ready · Validation#4 blocked on HWP D-export
- current_mode: EXAM
- exam_impact_score: 5
- estimated_hours: 2–6 (Human export + join tooling)
- study_roi: HIGH
- recommended_action: DO_NOW
- learning_goal: { subject: ACC, chapter: GOLDEN_2018_PILOT, pattern: AFTER_QDB }
- success_metric:
  1. VERIFY_EXPORT status=attested for 2018/answer
  2. Q41–Q80 answer join ≥ 40/40 (or documented gaps only with Human note)
  3. source sha256 unchanged · Product/Parser/OCR untouched
- proposed_files_in:
  - data/knowledge/raw/hwp-export/2018/answer/**
  - data/knowledge/raw/attested/2018/answer/**
  - data/knowledge/pilot/2018/candidate/ACC_2018_Q0{41..80}.json  # answer field fill
  - data/knowledge/pilot/2018/verify/answer-join-report.md
  - data/knowledge/pilot/2018/verify/human-verify-checklist.md     # update
- proposed_files_out:
  - source/**
  - scripts/parser/**
  - data/question-db*.json
  - data/pattern-db*.json
  - 2019–2026 outputs
- forbidden_actions:
  [ocr, parser-edit, pattern-db, question-db-product, d3-write, d4-write,
   --apply, source-edit, convert, year-expand, promote-probe-to-attested]
- human_gates_required: [VERIFY_EXPORT, join spot-check, B]
```

---

## Procedure

### Phase A — Human VERIFY_EXPORT (필수)

1. Open `source/past-exams/2018/answer.hwp` **read-only** (원본 저장/변환 금지).
2. Export lossless answer key (ACC 관련 정답 · Q41–Q80 포함) →  
   `data/knowledge/raw/hwp-export/2018/answer/`
3. Write `answer.export.json` per WO-010 schema (`source.sha256` = inventory).
4. Human checklist: spot-check vs HWP → `verification.status=attested`.
5. Promote metadata/files to `data/knowledge/raw/attested/2018/answer/`.

### Phase B — Human Answer Manifest (현행)

1. Create `answer.tsv` + `answer-manifest.json` (Q41–Q80 scaffold).
2. Human fills TSV from `answer.hwp` (OCR 금지 · parser 복사 금지).
3. On `MANIFEST FILLED WO-012` → join candidates · attested promote.
4. Emit join report (joined / missing / ambiguous).

### Phase B-join — Answer Join (Engineer · manifest filled 이후)

1. Parse `answer.tsv` → `{ questionNo: answerKey }` for **41–80**.
2. Patch `data/knowledge/pilot/2018/candidate/ACC_2018_Q0XX.json` `answer` field only.
3. Update `answer-join-report.md` + checklist.

### Phase C — Verification

- Join count target: **40/40** ACC Q41–Q80  
- Mismatch / missing → FAIL itemized · no silent fill  
- Validation #4 → PASS only if join complete + Human spot-check

---

## Success Criteria

| # | Criterion | Pass |
|---|-----------|------|
| 1 | VERIFY_EXPORT attested | `raw/attested/2018/answer/` + export.json status |
| 2 | source hash bind | sha256 match WO-009 |
| 3 | Q41–Q80 join | 40/40 (or Human-accepted gap list) |
| 4 | Scope Lock | OUT 항목 0건 위반 |
| 5 | Probe non-promotion | probe output not used as attested |

---

## Chain

```text
WO-011 pilot candidates (answer=null)  [exists]
  → 00_Navigator  ✅ WO-012 발행
  → ★ Human VERIFY_EXPORT (Phase A)   ← 현재 대기
  → 02_Engineer join (Phase B)
  → 03_Verification
  → ★ Gate B / VERIFY_QUESTION 계속
```

---

## Human kickoff

Export 적재 후 회신 예:

`EXPORT READY WO-012 path=data/knowledge/raw/hwp-export/2018/answer/`
