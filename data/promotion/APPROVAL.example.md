# Promotion Approval

승격 시에만 이 파일을 `APPROVAL.md`로 복사하고 아래를 채운다.

```
APPROVED: YES
Approver: <name>
Date: YYYY-MM-DD
Scope: full | canary
Notes: Display Acceptance Gate 통과 근거
```

`APPROVED: YES` 문자열이 없으면 `--apply`가 거부된다.
